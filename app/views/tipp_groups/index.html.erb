<div class="col-md-8">
  <div class="well">
    <% if flash[:notice] %>
      <p class="alert alert-info"><%= notice %></p>
    <% end %>
    <h3>Alle Tipp-Gruppen</h3>

    <table class="table table-striped">
      <thead>
        <tr>
          <th>Gruppenname</th>
          <th>Gruppengründer</th>
          <th></th>
        </tr>
      </thead>

      <tbody>
        <% @tipp_groups.each do |tipp_group| %>
          <tr>
            <td><%= tipp_group.name %></td>
            <td><%= tipp_group.user.username %></td>
            <td><%= link_to 'Details anzeigen', tipp_group, class: "btn btn-primary btn-sm pull-right" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to 'Neue Tipp-Gruppe erstellen', new_tipp_group_path, class: "btn btn-primary" %>
  </div>
</div>